# 映写室（projection-room）仕様

## プロジェクト
古い映画の本物の映像と、AIが生成した偽物の映像を見分ける、ホラー風のWebゲーム。
スマホの縦画面で遊ぶことを主に想定し、PCでも遊べるようにする。

## 開発環境（Docker）
ローカルに Node.js を入れずに開発できるようにする。
- Dockerfile：node:22-alpine ベース。ffmpeg も入れる（ダミー動画の生成と素材の加工に使うため）
- docker-compose.yml：
  - ソースをバインドマウントし、node_modules は名前付きボリュームにする
  - Vite の開発サーバーを 5173 で公開し、--host 0.0.0.0 で起動して同じWi-Fiのスマホから実機確認できるようにする
  - 素材フォルダ（clips/）もマウントする
- README.md に、docker compose up で起動、スマホから http://<PCのIP>:5173 で確認、という手順を書く
- npm script：dev / build / preview / test / lint / gen:dummy（ダミー動画とclips.jsonの生成）
- テストと Lint もコンテナ内で実行できるようにする

## 技術構成
- Vite + React + TypeScript、バックエンドなしの静的サイト
- 状態管理は軽量なもの（Zustand など）
- ゲームのルール（出題の選択、巻とループ、エンディング判定）は UI から完全に分離した純粋な TypeScript にし、Vitest でユニットテストを書く
- ゲーム全体の流れはステートマシンで管理する（タイトル / プレイ中 / ループ演出 / エンディング / 振り返り / エンディング一覧 / 設定）
- PWA 対応（ホーム画面に追加できるようにする）
- 既存の他作品の固有の表現・名称・デザインは真似しない

## 世界観
深夜の古い映画館の映写室。プレイヤーは映写技師。
上映するフィルムに偽物（AI動画）が紛れ込んでいる。本物は映写し、偽物は焼き捨てる。
判断を誤ると映写機が止まり、第1巻からやり直しになる。全8巻を通せば夜明けを迎えて脱出できる。

## 画面構成（縦画面）
- 中央に 4:3 の映像を幅いっぱいで表示する。上下の余白は映画館の暗闇として演出に使う
- 上の余白：現在の巻の表示、映写機（リールが回る。音と連動）
- 下の余白：「← 焼き捨てる／映写する →」の操作案内（数回プレイ後は薄くする）
- 映像の周りにフィルムの縁（パーフォレーション）を描き、ゆっくり流す
- iOS のセーフエリア（env(safe-area-inset-bottom)）を考慮する
- PC では画面中央に縦長のゲーム領域を置き、左右は暗闇で埋める

## 基本ルール
- 動画は1本10秒。1回だけ再生され、終わると最後のフレームで停止する
- 右スワイプ＝本物（映写する）、左スワイプ＝AI（焼き捨てる）。PCでは左右矢印キーとマウスドラッグでも操作できる
- スワイプ中はカードが傾き、左右に「焼き捨てる」「映写する」の表示がうっすら出る
- 時間制限はない。答えるまで待つ
- 上部の映写機アイコン（または映像のタップ）でリプレイできる。リプレイに罰はなく、回数は無制限
- 回答すると間を置かず次の動画へ進む。正解・不正解の表示はせず、画面端のノイズや光など流れを止めない反応だけにする
- 1巻につき1本を正しくさばくと次の巻へ進む。連続8本を正しくさばいて第8巻を通過すると脱出
- 1回でもミスしたら第1巻に戻る。演出は「映写機が止まる音 → 暗転 → 巻き戻し音 → 第1巻の字幕カード」。説明文は出さない
- 巻が変わる節目で2〜3秒の暗転と、無声映画風の字幕カードを出す（文言はデータで管理）
- 映像の見え方はループ回数に関係なく常に同じにする（視界を悪くする演出は入れない）
- ループ回数は「字幕カードの文言」「上下の余白に現れる人影の距離」「映写機の音に混ざる異音」だけで表現する

## 不穏タイマー
- 1本の動画が表示されてからの経過時間を計測する。数字は表示しない
- リプレイしてもリセットせず、回答した時点でリセットする
- 経過に応じて段階的に演出を変える（初期値：20秒 / 35秒 / 50秒 / 60秒）
  映写機の音の速さと重さ、上部余白の人影の濃さと距離、下部余白の息づかいの音量、映写機の光の強さ
- 映像の中央60%には演出を重ねず、判断を妨げない
- 60秒に達したら映写機が止まって暗転し、「暗闇エンド」としてプレイを終了する（第1巻には戻らない）
- 各段階の秒数と演出の強さは設定ファイルで調整できるようにする

## 出題ロジック
- 本物とAIをランダムに出題する。難易度による出し分けはしない
- 1回のプレイ（脱出または終了まで）で同じ動画は繰り返さない。在庫が尽きたら出題済みをリセットする
- 同じ種類（本物／AI）が5本以上連続しないようにする
- 同じ元作品の場面が連続しないようにする
- 次の2本を先読みし、切り替えで再生が止まらないようにする

## 動画データ
- 動画は public/clips/ に置く。ファイル名はランダムなIDにし、答えが分かる名前にしない
- メタデータは clips.json で管理する：
  { "id": "k8f3x2", "src": "/clips/k8f3x2.mp4", "isAI": false,
    "title": "作品名", "year": 1922, "director": "監督名",
    "sourceUrl": "入手元URL", "license": "ライセンス表記",
    "tool": "生成ツール（AIの場合）", "note": "振り返り画面に出す解説" }
- MP4（H.264）、無音、muted・playsinline 属性で自動再生する
- 素材がまだないので、ffmpeg でダミー動画（10秒・4:3・無音・動きのあるテストパターン）を本物役10本・AI役10本生成するスクリプトと、対応する clips.json を用意する（npm run gen:dummy）

## エンディング
判定はプレイ中の統計（累計ループ回数、本物を焼いた回数、AIを見逃した回数、1本あたりの平均リプレイ回数など）で行う。
条件と文章は後から編集しやすいようにデータで持たせる。文章は仮のもので構わない。
- 夜明けエンド：第8巻を通過した（通常クリア）
- 真エンド：一度もループせずに通過した
- 終わらない上映エンド：累計ループが一定回数（初期値20）を超えた状態で通過した
- 擦り切れたフィルムエンド：1本あたりの平均リプレイ回数が一定以上の状態で通過した
- 暗闇エンド：不穏タイマーが最後まで進んだ
エンディングごとに、字幕カード風のテキストを数枚表示し、演出用動画があれば再生できるようにする。

## エンディング後
- 振り返り画面：そのプレイで間違えた動画を再生し、本物なら作品名・公開年・出典、AIなら解説メモを表示する
- エンディング一覧画面：未達成は「？？？」と条件のヒントだけ表示する
- 記録は localStorage に保存し、読み書きは try/catch で囲んで失敗しても遊べるようにする
- 記録を短い文字列で書き出し・読み込みできる「セーブコード」機能を付ける

## 音
- Web Audio API を使う。映写機の回転音ループと低いドローン音を常時鳴らす
- 不穏タイマーの段階で音の速さ・重さを変える。巻の節目では映写機の音を一瞬止める
- 正解時の小さな音、ミス時の不協和音、巻き戻し音
- 左右スワイプに合わせて音を左右に振る（StereoPannerNode）
- 音声ファイルはまだないので Web Audio で合成した仮の音にし、後から差し替えられるようにする
- スマホの制限に対応し、タイトル画面の「上映を始める」ボタンで音声を有効化する
- 設定で音量とミュートを変えられるようにする

## 演出（最後にまとめて実装）
最も作り込むのは「古いフィルムが映写されている質感」。他は控えめにする。
- 映像全体をランダムに0〜2px揺らし、数十秒に一度だけ上下に数px跳ねる
- 明るさを1〜3%の範囲でゆっくり揺らす（点滅に見えない強さ）
- 動画が切り替わる瞬間に、2〜4フレームだけリーダーフィルム風の数字や白い傷を挟む
- ミス時：映像中央から焦げ跡が広がって穴が開く → 高速の巻き戻し → 第1巻の字幕カード
- 上下の余白に、低頻度（数分に1回程度）で人影が横切る
- 各演出の強さは1か所の設定ファイルで調整できるようにする

## アクセシビリティ
- 初回起動時に「点滅や暗い映像を含む」注意を表示する
- 強い明滅は使わない
- 設定の「点滅を弱める」で、揺れ・明滅・切り替えの一瞬・人影をまとめて無効化する
- 設定の「演出を軽くする」で、Canvas の重い演出を簡易版に切り替える

## 画面一覧
タイトル（上映を始める / エンディング一覧 / 設定 / クレジット）、ゲーム、エンディング、振り返り、
エンディング一覧、設定（音量・演出の強さ・点滅を弱める）、クレジット（clips.json の出典とライセンス、使用音源の一覧）

## 進め方
いきなり全部作らず、次の順で進め、各段階で動く状態にしてコミットする。
1. Docker環境、Vite+React+TS の雛形、ダミー動画と clips.json の生成、1本ずつ再生されて左右スワイプで判定できる最小版
2. リプレイ、巻とループ、先読み、出題ロジックとそのテスト
3. 不穏タイマーと段階演出、音
4. マルチエンディング、振り返り、エンディング一覧、セーブコード
5. タイトル・設定・クレジット・注意表示、フィルムの質感演出、PWA化、スマホ実機での調整

## 実装上の決定事項（計画時に確定）
- 1巻あたりの本数は 1 本（当初 3 本で実装したあと変更）。連続8本の正解で脱出する
- ダミー動画は本物役12本／AI役12本＝24本。1周に必要なのは8本なので十分な余裕がある
- 配信先は未定のため相対パス構成（Vite の base は './'、clips.json の src は 'clips/xxx.mp4' 形式で保持し実行時に BASE_URL と連結）
- 実素材の劣化処理スクリプト（npm run prep:clip）も用意する
- PWA はアプリ本体のみ事前キャッシュし、動画は実行時キャッシュ
- clips.json に `work`（元作品グループ）フィールドを追加する。「同じ元作品の場面が連続しない」判定に必要
