# （仮）Uncanny Feed 仕様

## プロジェクト
ショート動画アプリのように見える、ホラー風のWebゲーム。
縦長の映像が次々に流れてくる中に、AIが生成した偽物の映像が紛れ込んでいる。
プレイヤーは本物と偽物を見分けて、左右にスワイプしていく。
スマホの縦画面で遊ぶことを主に想定し、PCでも遊べるようにする。

## 開発環境（Docker）
ローカルに Node.js を入れずに開発できるようにする。
- Dockerfile：node:22-alpine ベース。ffmpeg も入れる（ダミー動画の生成と素材の正規化に使う）
- docker-compose.yml：
  - ソースをバインドマウントし、node_modules は名前付きボリュームにする
  - Vite の開発サーバーを 5173 で公開し、--host 0.0.0.0 で起動して同じWi-Fiのスマホから実機確認できるようにする
  - 素材の作業フォルダ（raw/）もマウントする
- README.md に起動手順（docker compose up、スマホから http://<PCのIP>:5173）を書く
- npm script：dev / build / preview / test / lint / gen:dummy / normalize
- テストと Lint もコンテナ内で実行できるようにする

## 技術構成
- Vite + React + TypeScript、バックエンドなしの静的サイト
- 状態管理は軽量なもの（Zustand など）
- ゲームのルール（出題の選択、段階とループ、エンディング判定）は UI から完全に分離した純粋な TypeScript にし、Vitest でユニットテストを書く
- ゲーム全体の流れはステートマシンで管理する（起動 / プレイ中 / ループ演出 / エンディング / 振り返り / エンディング一覧 / 設定）
- PWA 対応（ホーム画面に追加できるようにする）

## 世界観
一見どこにでもある、架空のショート動画アプリ。
滝、猫、街、コーヒーなど、何の変哲もない綺麗な映像が流れてくる。
しかしフィードには「誰も撮っていない映像」が紛れ込んでいる。
本物は残し、偽物は報告して取り除く。見誤るとおすすめがリセットされ、最初からやり直しになる。
8段階を通し切れば、このフィードから抜け出してアプリを閉じられる。
普段見慣れた画面が、少しずつおかしくなっていく怖さを目指す。

## デザイン上の注意
- どこかで見たような雰囲気の、架空のアプリとして作る
- 実在のアプリ（YouTube、TikTok、Instagram など）のロゴ、アプリ名、配色、アイコン、特徴的なレイアウトをそのまま真似しない
- アプリ名は仮に「（仮）Uncanny Feed」とし、あとで変えられるよう1か所で定義する
- アイコンは lucide-react などの汎用アイコン、または自作のシンプルな図形を使う

## 画面構成（縦画面）
- 映像は 9:16 で画面いっぱいに表示する（object-fit: cover）
- ショート動画アプリ風のUIを映像の上に重ねる
  - 右側の縦並びアイコン：リプレイ、ハート（飾り）、コメント数（飾り）、共有（飾り）
  - 下部：投稿者名風の表示（本物は提供者名、AIは架空のアカウント名。どちらも同じ書式にして見分けの手がかりにしない）、短いキャプション
  - 上部：段階の進捗（8つの小さなバー）
  - 画面下の端に「← 報告する／残す →」の操作案内（数回プレイ後は薄くする）
- iOS のセーフエリア（env(safe-area-inset-*)）を考慮する
- PC では画面中央に 9:16 のゲーム領域を置き、左右は暗い背景で埋める

## 基本ルール
- 動画は1本10秒。1回だけ再生され、終わると最後のフレームで停止する
- 右スワイプ＝本物（残す）、左スワイプ＝AI（報告する）。PCでは左右矢印キーとマウスドラッグでも操作できる
- スワイプ中はカードが傾き、左右に「報告」「残す」の表示がうっすら出る
- 時間制限はない。答えるまで待つ
- 右側のリプレイアイコン（または映像のタップ）でリプレイできる。罰はなく、回数は無制限
- 回答すると間を置かず次の動画へ進む。正解・不正解は表示せず、画面が一瞬揺れる・通知音が鳴るなど流れを止めない反応だけにする
- 1段階につき1本を正しくさばくと次の段階へ進む（連続8本で脱出）
- 1回でもミスしたら第1段階に戻る。演出は「画面が一瞬固まる → 読み込み中の表示 → おすすめがリセットされました、という通知 → 第1段階」。説明文は出さない
- ループ回数は映像そのものには影響させない。次のような「アプリのおかしさ」だけで表現する
  - キャプションの文言が少しずつ不穏になる（文言はデータで管理）
  - 投稿者名に同じ名前が増えていく
  - 通知バッジの数字が増える
  - ハートやコメントの数が不自然な値になる

## 不穏タイマー
- 1本の動画が表示されてからの経過時間を計測する。数字は表示しない
- リプレイしてもリセットせず、回答した時点でリセットする
- 経過に応じて段階的に演出を変える（初期値：20秒 / 35秒 / 50秒 / 60秒）
  - 画面の明るさがゆっくり落ちる
  - ハートやコメントの数が勝手に増え続ける
  - 通知が次々に届く（内容は意味深なもの）
  - 低い環境音が大きくなる
- 映像の中央部分には演出を重ねず、判断を妨げない
- 60秒に達したら画面が真っ暗になり、「暗転エンド」としてプレイを終了する（第1段階には戻らない）
- 各段階の秒数と演出の強さは設定ファイルで調整できるようにする

## 出題ロジック
- 本物とAIをランダムに出題する。難易度による出し分けはしない
- 1回のプレイ（脱出または終了まで）で同じ動画は繰り返さない。在庫が尽きたら出題済みをリセットする
- 同じ種類（本物／AI）が5本以上連続しないようにする
- 同じカテゴリが3本以上連続しないようにする
- 次の2本を先読みし、切り替えで再生が止まらないようにする

## 動画データ
- 配信する動画は public/clips/ に置く。ファイル名はランダムなIDにし、答えが分かる名前にしない
- すべて 720×1280、24fps、10秒、無音、H.264、faststart に揃える
- メタデータは clips.json で管理する：
  { "id": "k8f3x2", "src": "clips/k8f3x2.mp4", "isAI": false,
    "category": "自然・風景", "scene": "滝",
    "source": "Pexels", "sourceUrl": "入手元URL", "contributor": "提供者名",
    "tool": "生成ツール（AIの場合）", "note": "振り返り画面に出す解説" }
- 動画は muted・playsinline 属性で自動再生する
- 素材がまだないので、ffmpeg でダミー動画（10秒・9:16・無音・動きのあるテストパターン）を本物役10本・AI役10本生成するスクリプトと、対応する clips.json を用意する（npm run gen:dummy）
- 素材の正規化スクリプトを用意する（npm run normalize）：raw/real/ と raw/ai/ の動画を、中央切り取りで 9:16 にし、720×1280・24fps・10秒・無音に揃えて、ランダムなIDで public/clips/ に書き出し、clips.json の雛形を更新する。元ファイル名と生成したIDの対応表は別ファイルに残す（公開しない）

## エンディング
判定はプレイ中の統計（累計ループ回数、本物を報告した回数、AIを見逃した回数、1本あたりの平均リプレイ回数など）で行う。
条件と文章は後から編集しやすいようにデータで持たせる。文章は仮のもので構わない。
- 通常エンド：第8段階を通過し、アプリを閉じられた
- 真エンド：一度もループせずに通過した。偽物の映像を誰が作っていたのかが分かる
- 無限スクロールエンド：累計ループが一定回数（初期値20）を超えた状態で通過した。閉じたはずのアプリが、また最初の動画から始まる
- 見すぎエンド：1本あたりの平均リプレイ回数が一定以上の状態で通過した
- 暗転エンド：不穏タイマーが最後まで進んだ。真っ暗になった画面に、何かが映る
エンディングごとに、通知やメッセージ風のテキストを数枚表示し、演出用動画があれば再生できるようにする。

## エンディング後
- 振り返り画面：そのプレイで間違えた動画を再生し、本物なら提供者・出典、AIなら解説メモを表示する
- エンディング一覧画面：未達成は「？？？」と条件のヒントだけ表示する
- 記録は localStorage に保存し、読み書きは try/catch で囲んで失敗しても遊べるようにする
- 記録を短い文字列で書き出し・読み込みできる「セーブコード」機能を付ける

## 音
- 動画は常に無音。ゲームの音は Web Audio API で別に鳴らす
- 通知音、スワイプ音、ミス時の違和感のある音、リセット時の読み込み音、不穏タイマーに合わせて大きくなる低い環境音
- 左右スワイプに合わせて音を左右に振る（StereoPannerNode）
- 音声ファイルはまだないので Web Audio で合成した仮の音にし、後から差し替えられるようにする
- スマホの制限に対応し、起動画面の「はじめる」ボタンで音声を有効化する
- 設定で音量とミュートを変えられるようにする

## アクセシビリティ
- 初回起動時に「点滅や暗い映像、不安をあおる表現を含む」注意を表示する
- 強い明滅は使わない
- 設定の「演出を弱める」で、揺れ・明滅・通知の連続表示をまとめて弱める

## 画面一覧
起動画面（アプリ風のスプラッシュ → はじめる / エンディング一覧 / 設定 / クレジット）、フィード（ゲーム本体）、
エンディング、振り返り、エンディング一覧、設定、クレジット（clips.json の提供者・出典の一覧と、使用音源の一覧）

## 進め方
1. Docker環境、Vite+React+TS の雛形、ダミー動画と clips.json の生成、9:16全画面で1本ずつ再生されて左右スワイプで判定できる最小版
2. リプレイ、段階とループ、先読み、出題ロジックとそのテスト
3. ショート動画アプリ風のUI、不穏タイマーと段階演出、音
4. マルチエンディング、振り返り、エンディング一覧、セーブコード
5. 起動画面・設定・クレジット・注意表示、正規化スクリプト、PWA化、スマホ実機での調整

## 実装上の決定事項
- 1段階あたり1本。連続8本の正解で脱出（当初の仕様の3本から変更）
- 映写室（白黒フィルム）版から作り替えたもの。出題ロジック・段階とループ・不穏タイマー・
  エンディング判定・セーブ・Docker・テストの仕組みはそのまま引き継いでいる
- 白黒版の資産（動画48本、質感を揃える film-look などのスクリプト群）は削除した。
  カラーでも本物とAIの質感差が問題になる場合は git の履歴から取り戻せる
